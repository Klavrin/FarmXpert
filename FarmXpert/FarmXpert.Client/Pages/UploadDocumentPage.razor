@page "/upload/{title}"

<MudContainer MaxWidth="MaxWidth.Medium" Class="mt-6">

    <MudText Typo="Typo.h5" Class="mb-4">@Title</MudText>

    <MudText Typo="Typo.body2" Class="mb-4">
        Please upload the required document below.
    </MudText>

    <MudPaper Elevation="2" Class="p-4 mb-4 text-center">
        <img src="images/Extras_202504.jpg" style="max-width: 100%; height: auto;" />
    </MudPaper>

    <MudButton Variant="Variant.Filled" Color="Color.Primary"
               StartIcon="@Icons.Material.Filled.CloudUpload"
               HtmlTag="label"
               for="fileInput">
        Upload Document
    </MudButton>
    
    <InputFile id="fileInput" OnChange="OnUpload" hidden />

</MudContainer>

@code {
    [Parameter] public string Title { get; set; } = "";

    private async Task OnUpload(InputFileChangeEventArgs e)
    {
        var file = e.File;
        Console.WriteLine($"Uploaded: {file.Name}");
    }
}